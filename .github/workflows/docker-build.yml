name: Build & Push Docker Image

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  IMAGE_NAME: gcc-cpp-rules-lab

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # ── Build image ──────────────────────────────────────────────────────────
      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          tags: ${{ env.IMAGE_NAME }}:latest
          cache-from: type=gha
          cache-to:   type=gha,mode=max

      # ── Smoke-test: compile and run all examples ─────────────────────────────
      - name: Run all examples
        run: |
          docker run --rm ${{ env.IMAGE_NAME }}:latest bash -c "./run_all.sh"

      # ── (Optional) Push to Docker Hub on main ───────────────────────────────
      # Uncomment and set DOCKER_USER / DOCKER_TOKEN secrets to enable pushing.
      #
      # - name: Log in to Docker Hub
      #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      #   uses: docker/login-action@v3
      #   with:
      #     username: ${{ secrets.DOCKER_USER }}
      #     password: ${{ secrets.DOCKER_TOKEN }}
      #
      # - name: Push to Docker Hub
      #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
      #   uses: docker/build-push-action@v5
      #   with:
      #     context: .
      #     push: true
      #     tags: ${{ secrets.DOCKER_USER }}/${{ env.IMAGE_NAME }}:latest
